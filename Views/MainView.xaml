<UserControl x:Class="HomeoMahanagarLabelCleanV2.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1280">

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- menu removed from top span; moved into right panel to avoid overlapping preview -->

        <!-- left: medicines table -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0" Margin="0,4,0,8" Height="28"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <DataGrid x:Name="MedicinesGrid" Grid.Row="1" AutoGenerateColumns="False" HeadersVisibility="Column" GridLinesVisibility="All"
                      CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" IsReadOnly="True"
                      SelectedCellsChanged="DataGrid_SelectedCellsChanged" MouseUp="MedicineCell_Clicked"
                      ItemsSource="{Binding MedicinesView}" SelectedItem="{Binding SelectedMedicine, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Latin Name" Binding="{Binding LatinName}" Width="*" />
                    <DataGridTextColumn Header="Common Name" Binding="{Binding CommonName}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                <Button Content="+ Add Medicine" Width="140" Margin="8,0" Command="{Binding AddMedicineCommand}" />
                <Button Content="Delete Medicine" Width="140" Margin="8,0" Command="{Binding DeleteMedicineCommand}" />
            </StackPanel>
        </Grid>

        <!-- right: details + preview -->
        <StackPanel Grid.Column="1" Margin="24,0,24,0">
            <DockPanel LastChildFill="False" HorizontalAlignment="Right">
                <Menu DockPanel.Dock="Top" HorizontalAlignment="Right" Width="220">
                    <MenuItem Header="Control">
                        <MenuItem Header="Upload Excel..." Command="{Binding UploadExcelCommand}" />
                    </MenuItem>
                    <MenuItem Header="Preview">
                        <MenuItem Header="Font family">
                            <MenuItem>
                                <ComboBox Width="160" SelectedValuePath="Content" SelectedValue="{Binding Preview.FontFamily}">
                                    <ComboBoxItem>Segoe UI</ComboBoxItem>
                                    <ComboBoxItem>Arial</ComboBoxItem>
                                    <ComboBoxItem>Calibri</ComboBoxItem>
                                    <ComboBoxItem>Times New Roman</ComboBoxItem>
                                </ComboBox>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="Font size">
                            <MenuItem>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="-" Width="28" Height="20" Click="PreviewFontSizeDecrease_Click" />
                                    <TextBox x:Name="PreviewFontSizeBox" Width="48" Height="20" Text="9" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="6,0,6,0" LostFocus="PreviewFontSizeBox_LostFocus" KeyDown="PreviewFontSizeBox_KeyDown" />
                                    <Button Content="+" Width="28" Height="20" Click="PreviewFontSizeIncrease_Click" />
                                </StackPanel>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="Line spacing">
                            <MenuItem>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBox Width="48" Height="20" Text="{Binding Preview.LineSpacing, Mode=TwoWay}" />
                                </StackPanel>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="Style">
                            <MenuItem>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="Bold" IsChecked="{Binding Preview.IsBold, Mode=TwoWay}" />
                                    <CheckBox Content="Vector PDF" IsChecked="{Binding Preview.ExportVectorPdf, Mode=TwoWay}" Margin="8,0,0,0" />
                                </StackPanel>
                            </MenuItem>
                        </MenuItem>
                    </MenuItem>
                </Menu>
            </DockPanel>

            <TextBlock Text="MEDICINE DETAILS" FontSize="26" FontWeight="Bold" Margin="0,4,0,12" />

            <StackPanel Margin="0,0,0,8">
                <TextBlock Text="Potency" />
                <ComboBox x:Name="PotencyBox" Height="26" IsEditable="True" ItemsSource="{Binding Potencies}" Text="{Binding Preview.Potency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Dose" Margin="0,8,0,0" />
                <ComboBox x:Name="DoseBox" Height="26" IsEditable="True" ItemsSource="{Binding Doses}" Text="{Binding Preview.Dose, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Time" Margin="0,8,0,0" />
                <ComboBox x:Name="TimeBox" Height="26" IsEditable="True" ItemsSource="{Binding Times}" Text="{Binding Preview.Time, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Shop Name" Margin="0,8,0,0" />
                <TextBox x:Name="ShopNameBox" Text="{Binding Preview.ShopName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Branch / Phone" Margin="0,8,0,0" />
                <TextBox x:Name="BranchBox" Text="{Binding Preview.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <TextBlock Text="Label Preview" FontSize="18" FontWeight="Bold" Margin="0,12,0,8" />

            <!-- Preview sized to physical label: 50mm x 30mm -> ~189 x 113 DIPs -->
            <!-- Outer container to provide subtle shadow and spacing -->
            <Border x:Name="PreviewContainerBorder" HorizontalAlignment="Left" VerticalAlignment="Top" Width="197" Height="121"
                    Background="White" CornerRadius="10" Padding="0" BorderBrush="#111" BorderThickness="1.0" Margin="0">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.18" />
                </Border.Effect>
                <Canvas x:Name="PreviewCanvas" Background="Transparent" ClipToBounds="True" Width="193" Height="117" />
            </Border>

            

            <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                <Button x:Name="PrintButton" Content="Print Label" Background="#0078D7" Foreground="White" FontSize="18" FontWeight="Bold"
                        Height="52" Width="220" Margin="0,0,16,0" HorizontalAlignment="Left" Command="{Binding PrintLabelCommand}" />
                <Button Content="PDF" Height="52" Width="120" Command="{Binding ExportPdfCommand}" CommandParameter="{Binding ElementName=PreviewContainerBorder}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,18,0,0" VerticalAlignment="Center">
                <Ellipse Width="12" Height="12" Fill="Green" VerticalAlignment="Center" />
                <TextBlock Text="Printer available" Margin="8,0,24,0" VerticalAlignment="Center" />
                <Ellipse Width="12" Height="12" Fill="Red" VerticalAlignment="Center" />
                <TextBlock Text="50×30 mm set" Margin="8,0,24,0" VerticalAlignment="Center" />
                <Ellipse Width="12" Height="12" Fill="Red" VerticalAlignment="Center" />
                <TextBlock Text="USB connected" Margin="8,0,0,0" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Hidden / auxiliary controls used by code-behind for preview editing -->
            <ComboBox x:Name="PreviewLineCombo" Visibility="Collapsed" SelectionChanged="PreviewLineCombo_SelectionChanged" />
            <TextBox x:Name="PreviewLineSpacingBox" Visibility="Collapsed" />
            <TextBox x:Name="PreviewSpacingSpinner" Visibility="Collapsed" />
        </StackPanel>
    </Grid>
</UserControl>
