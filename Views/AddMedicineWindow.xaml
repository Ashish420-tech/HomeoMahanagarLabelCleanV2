<Window x:Class="HomeoMahanagarLabelCleanV2.Views.AddMedicineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Medicine"
        Width="350"
        Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Latin Name"/>
        <TextBox x:Name="LatinBox"
                 Grid.Row="1"
                 Margin="0,5,0,10"/>

        <TextBlock Text="Common Name"
                   Grid.Row="2"/>
        <TextBox x:Name="CommonBox"
                 Grid.Row="3"
                 Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Add"
                    Width="80"
                    Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
